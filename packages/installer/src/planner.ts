import { digest } from "./digest.js";
import type { DesiredArtifact, InstallRoots, ManagedResourceState, UpdateCandidate, ComparisonKind, UpdateDecision } from "@skynex-internal/domain";
export function compareResource(artifact: DesiredArtifact, previous: ManagedResourceState | null, localBytes: Buffer | null, decision?: UpdateDecision): UpdateCandidate { const localDigest = localBytes ? digest(localBytes) : null; const upstream = artifact.sourceDigest ?? digest(artifact.content); const base = previous?.sourceDigest ?? null; let comparison: ComparisonKind; if (!localDigest && !previous) comparison = "create"; else if (localDigest && !previous) comparison = "unmanaged-collision"; else if (localDigest === base && upstream !== base) comparison = "upstream-only"; else if (localDigest !== base && upstream === base) comparison = "local-only"; else if (localDigest === upstream) comparison = "converged"; else comparison = "conflict"; return decision === undefined ? { artifact, previous, localDigest, comparison } : { artifact, previous, localDigest, comparison, decision }; }
